package com.google.appengine.api.labs.servers;

/**
 * Thrown when an application tries to start a server that is already started.
 *
 * @see ServersService#startServer
 */
public class ServerAlreadyStartedException extends ServersException {

  ServerAlreadyStartedException(String detail) {
    super(detail);
  }
}
