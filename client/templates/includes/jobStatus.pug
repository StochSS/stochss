div#job-status.card.card-body

  div

    h3.editor-header Job Status

    button.btn.btn-outline-collapse(data-toggle="collapse", data-target="#collapse-status", data-hook="collapse", disabled) +

  div.collapse(id="collapse-status", data-hook="job-status")

    table.table
      thead
        tr

          th(scope="col") Date
          th(scope="col") Status

      tbody
        tr

          td=this.startTime
          td=this.status

    button.btn.btn-outline-primary(data-hook="stop-job", type="button", disabled)
      | Stop Job

    button.btn.btn-outline-primary(data-hook="restart-job", type="button", disabled)
      | Restart Job