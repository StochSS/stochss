div#plots-view.card.card-body

    div

        h5.inline=this.heading
        button.btn.btn-outline-collapse(data-toggle="collapse", data-target="#collapse"+this.model.key+this.model.stamp, id="collapse-plot" data-hook="collapse-plot")

    div.collapse(class="show", id="collapse"+this.model.key+this.model.stamp)

        div(id=this.model.key+this.model.stamp data-hook=this.model.key+this.model.stamp)