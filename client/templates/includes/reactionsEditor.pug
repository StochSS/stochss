div#reactions-editor.card.card-body

  div

    h3.editor-header Reactions
    button.btn.btn-outline-collapse(data-toggle="collapse", data-target="#collapse-reaction", data-hook="collapse") -

  div.collapse(class="show", id="collapse-reaction")

    p
      | Define reactions. Select from the given reaction templates, or use the custom types. 
      | Using templated reaction types will help eliminate errors. 
      | For non-linear reactions, use the custom propensity type. 

    div.row
      div.col-md-6: table.table
        thead: tr
          td Edit
          td Name
          td Summary
          td Delete

        tbody(data-hook="reaction-list")

      div.col-md-6(data-hook="reaction-details-container")
      
    div.dropdown

      button.btn.btn-outline-primary.dropdown-toggle#addReactionBtn(
        data-hook="add-reaction-full",
        data-toggle="dropdown",
        aria-haspopup="true",
        aria-expanded="false",
        type="button"
      ) Add Reaction <span class="caret"></span>

      ul.dropdown-menu(aria-labelledby="addReactionBtn")
        li.dropdown-item(data-hook="creation")
        li.dropdown-item(data-hook="destruction")
        li.dropdown-item(data-hook="change")
        li.dropdown-item(data-hook="dimerization")
        li.dropdown-item(data-hook="merge")
        li.dropdown-item(data-hook="split")
        li.dropdown-item(data-hook="four")
        li.dropdown-divider
        li.dropdown-item(data-hook="custom-massaction") Custom mass action
        li.dropdown-item(data-hook="custom-propensity") Custom propensity

    div.dropdown

      button.btn.btn-outline-primary.dropdown-toggle#addReactionBtn(
        data-hook="add-reaction-partial",
        data-toggle="dropdown",
        aria-haspopup="true",
        aria-expanded="false",
        type="button"
      ) Add Reaction <span class="caret"></span>

      ul.dropdown-menu(aria-labelledby="addReactionBtn")
        li.dropdown-item(data-hook="custom-propensity") Custom propensity
