div#parameters-editor.card.card-body

  h3 Parameters Editor

  table.table
    thead
      tr
        th(scope="col") Name
        th(scope="col") Initial Condition
        th(scope="col") Remove
      
    tbody(data-hook="parameter-list")

  button.btn.btn-outline-primary(data-hook="add-parameter", type="button")
    | Add Parameter

